@model Red_Lake_Hospital_Redesign_Team6.Models.ViewModels.ShowEcard

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Ecards</h4>
    <div>
        <nav class="editnav">
            <ul>
                <li>@Html.ActionLink("Back to List", "List")</li>
                @if (Model.isadmin)
                {
                    <li>@Html.ActionLink("Delete", "DeleteConfirm", new { id = Model.ecard.ecard_id })</li>
                    <li>@Html.ActionLink("Edit", "Edit", new { id = Model.ecard.ecard_id })</li>
                }
            </ul>

        </nav>
        <hr />
        <dl class="dl-horizontal">


            <dt>
                @Html.DisplayNameFor(model => model.ecard.photo_path)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ecard.photo_path)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ecard.message)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ecard.message)
            </dd>

        </dl>
    </div>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ecard.ecard_id })
</p>
